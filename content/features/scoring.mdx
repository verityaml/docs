---
title: Bank-readiness scoring
description: Map regulatory requirements to scoring criteria, track evidence status, and compute readiness scores across compliance domains
---

<img
  src="/images/evidence-internal-controls-top.png"
  alt="Domain detail page showing scoring criteria with evidence status"
  style={{ borderRadius: '0.5rem' }}
/>

Bank-readiness scoring maps regulatory requirements to scoring criteria by compliance domain, tracks evidence status, and computes readiness scores. Each criterion links to its regulatory source in the knowledge base.

## How scoring works

The scoring model is a three-tier hierarchy: **Program** → **Domain** → **Criteria**.

- **Programs** are scoped by regulation type — BSA/AML maps to 5 FFIEC pillars, Third-Party Oversight maps to 8 OCC 2023-17 monitoring areas
- **Domains** correspond to regulatory categories (e.g., Internal Controls, Independent Testing)
- **Criteria** have an evidence status auto-derived from their score: `missing` (0 pts), `partial` (1–99 pts), or `sufficient` (100+ pts)

### Score formula (v2 — novelty-weighted additive)

Verity measures evidence *diversity*, not just evidence *quantity*. Each piece of evidence linked to a criterion is compared against all other linked evidence using cosine similarity on Voyage AI embeddings:

```
contribution = 100 × (1 − max_cosine_similarity_to_prior_evidence)
criterion_score = sum(contributions)    # unbounded
domain_score = sum(criterion_scores)    # unbounded
program_score = sum(domain_scores)      # unbounded
```

- First evidence linked to a criterion contributes **100 points** (full novelty)
- Near-duplicate evidence (high cosine similarity) contributes close to **0 points**
- Evidence that can't be embedded (non-text, failed extraction) contributes a flat **50 points**
- Identical files are blocked before linking — SHA-256 content-hash dedup returns 409

Scores are **unbounded integers** — a team that provides diverse, complementary evidence earns a higher score. Duplicate uploads don't inflate the score. Evidence status is **auto-derived** from the criterion score and cannot be set manually.

### Per-domain configurable thresholds

Each domain has an optional `threshold_pct` (1–100) that replaces the old global 70% threshold. Admins can set readiness targets per domain. Threshold-crossed notifications fire per-domain using each domain's configured target. The `REFERENCE_DEPTH_PTS` constant (240 — the score yielded by 10 fully diverse evidence per criterion) is used to convert percentage thresholds into absolute score targets.

### Evidence embedding pipeline

All evidence uploads compute a SHA-256 `content_hash`, set `embedding_status='pending'`, and dispatch to the `embed-evidence` SQS queue. The Lambda handler downloads the file from S3, runs `extractTextFromBuffer()`, and embeds via Voyage AI (`voyage-law-2`). Once embedded, it calls `recomputeCriterionScore()` for each linked criterion — scores update asynchronously after upload.

## Criteria table

The domain detail page lists every scoring criterion, each one mapped to a specific section of regulatory guidance:

| Column | Description |
|--------|-------------|
| Criterion text | What the regulation requires |
| Regulatory source | The specific FFIEC section or OCC 2023-17 paragraph |
| Evidence count | How many documents are linked |
| Criterion score | Unbounded novelty-weighted score |
| Evidence status | Auto-derived: Missing (0), Partial (1–99), or Sufficient (100+) |

Each row also shows the **criterion definition key** (e.g., `ic-01`, `tr-03`) in the Source column above the regulatory source link, making it easy to cross-reference with parsed examination items.

The status column is a dropdown — click to change it. A modal prompts for an optional reason ("Updated after board review," "Waiting on Q4 minutes") and the domain score updates immediately.

<Warning>
Criteria with a **REQUIRES REVIEW** warning beneath the status dropdown have been auto-matched with evidence and set to "sufficient" by the system, but no human has confirmed them yet. The warning clears when you change the status or add reviewer notes.
</Warning>

## Reviewer notes and history

Click **Expand** on any criterion to reveal two tabs: **Evidence** and **History**.

The **Evidence** tab shows reviewer notes (auto-saved after a one-second pause) and linked evidence documents.

The **History** tab is the audit trail — a scrollable, filterable log of every change to this criterion. Who changed the status, when, and why. System-generated entries from auto-match are logged as "System." Filter by change type (status, evidence, notes) to find what you need.

## Domain-level evidence upload

Above the criteria table, a **drag-and-drop upload zone** lets you upload evidence files directly to the domain without going through an examination workflow. Multiple files are supported with per-file progress indicators.

After upload, the system auto-suggests the uploaded evidence against all criteria in the domain. Unlinked files appear in an expandable section, and you can manually assign them to specific criteria using the **Link Evidence** dropdown on any criterion's Evidence tab.

## Linked evidence

<img
  src="/images/evidence-cdd-kyc.png"
  alt="Scoring criteria with linked evidence documents"
  style={{ borderRadius: '0.5rem' }}
/>

Below the notes, you'll see evidence documents linked to the criterion. Each entry shows:
- The document label
- Its source type (examination, domain upload, library, or remediation)
- An **AUTO** label on entries linked by the auto-match system
- A **Remove** button to unlink the entry

## Auto-matching

The **Find Evidence** button in the domain detail page header bridges examination workflow and compliance program scoring.

Click "Find Evidence" to open a dialog listing your organization's examinations with checkboxes. Select individual examinations or use **Select All**, then click **Match Selected**. The system scans each selected examination's request items and attached evidence against the criteria in the current domain.

From the dashboard, the **Run Gap Analysis** button matches evidence from all examinations across all domains at once.

Matching uses two signals:

1. **Direct criterion key** — if a request item has a scoring criterion key (like `ic-01`) that matches a criterion's definition key, that's an exact match
2. **Regulatory source alignment** — if a request item and a criterion both reference the same FFIEC section, the evidence is relevant

When evidence is auto-linked, the system sets the criterion's status to **Sufficient** and recomputes the domain score. The **REQUIRES REVIEW** warning appears until a human confirms. This is opt-out, not opt-in — the system attaches evidence it considers relevant, and you remove anything that doesn't belong.

<Tip>
Auto-matching turns examination evidence into reusable compliance proof. Evidence uploaded for one examination cycle can strengthen your readiness score across future cycles.
</Tip>

## Trends page

Navigate to **Trends** from the compliance program view to see score history over time.

The trends page at `/compliance/[programId]/trends` shows a Recharts line chart of score events with:

- **Date range presets** — 7d, 30d, 90d, 180d, 1 year
- **Auto-bucketing** — daily, weekly, or monthly aggregation based on the selected range
- **Domain filter** — scope the chart to a specific domain or view the full program
- **Paginated events table** — every score change with trigger type, delta, and timestamp

Pre-v2 historical scores (from before the novelty-weighted scoring migration) render as a **dashed clay-coloured series** on a secondary axis, so you can see the full scoring history across the formula transition. Domain card sparklines also show the v1 segment as a dashed line preceding the solid v2 line.

All score events are tagged with a `formula_version` (1 or 2) to prevent mixing percentage-based legacy scores with unbounded v2 scores on the same axis.
