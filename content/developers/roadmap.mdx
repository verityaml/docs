---
title: Roadmap
description: Planned features and tracked improvements beyond the current build
---

## Spec status

All 38 feature specs are tracked in the main repository's `SPECLOG.md`. Current status as of June 2026:

| Spec | Status |
|------|--------|
| Platform | Complete |
| Design System | Complete |
| Auth & Tenancy | Complete |
| Knowledge Base | Complete |
| Exam Parser | Complete |
| Examination Workflow Tracker | Complete |
| Response Archive | Complete |
| ~~Docker Demo~~ | Removed |
| Bank-Readiness Scoring | Complete |
| Regulatory Reference Library | Complete |
| Evidence-Scoring Integration | Complete |
| Report Generation | Complete |
| Consent Order Parsing | Complete |
| Evidence Linking | Complete |
| Control Center Update | Complete |
| Audit Trail | Complete |
| Domain Evidence Upload | Complete |
| Score Snapshots | Complete |
| Evidence Library | Complete |
| Readiness Report Export | Complete |
| AWS Infrastructure | Complete |
| AWS Database Migration | Complete |
| AWS Storage Migration | Complete |
| AWS Background Jobs | Complete |
| AWS E2E Pipeline | Complete |
| AWS CI/CD Consolidation | Complete |
| AWS Provider Swap + Cutover | Complete |
| Reference-Based Extraction | Complete |
| Event-Based Score Recomputation | Complete |
| Score Change UI & Notifications | Complete |
| Google Drive Connector | Complete |
| Agentic File Classification | Complete |
| Evidence Bridge | Complete |
| Desktop App | In Progress |
| Library Evidence Classification | In Progress |
| Classification FDR Tuning | In Progress |
| Funnel Eval Calibration | Specified |
| Scoring Refactor | Complete |

**33 complete**, 3 in progress, 1 specified, 1 removed.

---

## In progress

### Classification FDR Tuning

Replacing single-shot connector classification (60.8% FDR) with a two-stage funnel: embedding retrieval for recall, LLM binary confirmation for precision. Shared `embedAndMatch()` helper powers both the connector funnel and library classification. OAuth scope defaulted to `drive.readonly` for all new connections (enables content extraction). DOCX content extraction added via `mammoth` so Word documents now flow through the funnel instead of falling back to metadata-only. Reconnect banner shown when an existing connector lacks the `drive.readonly` scope. CI eval workflow gates changes to classification files. Target FDR ≤ 10%.

### Funnel Eval Calibration (Specified)

All six phases complete (PR #448 phases 1–4 tooling, PR #451 phases 5–6 calibration + docs). Closes issue #430. SPECLOG status remains "Specified" pending the deferred staging smoke job.

**Calibration result:** `RETRIEVAL_THRESHOLD` lowered from 0.55 to 0.50 — calibrated against the `verity-eval` org with `voyage-law-2` embeddings across 5 runs. At 0.50: ~87% accuracy, ~5% FDR on the golden set (49 expected matches across 41 cases), passing the ≥ 80% accuracy / ≤ 10% FDR merge gate. Haiku Stage 2 non-determinism causes ~20pp accuracy variance; 0.50 is the most stable operating point.

**Golden set enrichments:** 10 cases' `contentPreview` fields expanded from ~200 to ~800–1200 chars with realistic regulatory text matching production `getFileContent()` output. 13 valid "added" matches accepted, 5 unrealistic expectations removed, 3 criteria-ambiguity cases reclassified.

**Tooling (phases 1–4):** shared `criteria-embeddings.ts` helper wired into all 5 insert sites, lazy heal at sync entry, deploy-time ECS backfill, `verity-eval` seed org with `--check` and `--calibrate` modes, lint-gate rules `no-inline-sequential-id-generation` and `seed-scripts-use-embedMissingCriteria`.

### Library Evidence Classification

Auto-classifying evidence library uploads via embedding similarity. Text extraction (PDF, text/*), heading-based chunking, pgvector cosine matching against scoring criteria, auto-link on high-confidence matches with score updates propagated through `applyScoreDelta()`. New `classify-library-evidence` SQS queue and Lambda. Criteria embedding column + backfill script. Classification status badges in the evidence table (pending, classified, no match, skipped, error).

### Desktop App

**Done (phases 1–7, 10–11):**
- Onboarding flow, dashboard snapshot, system tray
- Cookie-based SSR auth, cross-origin token bootstrap
- Dev-mode polling auth fallback
- 12 post-deploy smoke tests

**Remaining:**
- Phase 0: Apple Developer enrollment
- Phase 8: Auto-updates
- Phase 9: CI/CD (code signing, DMG notarization)

---

## Recently completed

### Scoring Refactor (Complete)

Novelty-weighted additive scoring model replacing the percentage-based formula. Evidence embeddings via Voyage AI (`voyage-law-2`), cosine similarity novelty weighting, unbounded criterion/domain/program scores, per-domain configurable thresholds (replacing the global 70%), content-hash SHA-256 dedup, and an `embed-evidence` SQS + Lambda pipeline for asynchronous embedding. Trends page at `/compliance/[programId]/trends` with Recharts line charts, date range presets, auto-bucketing, and domain filtering. Historical v1 scores render as a dashed series on a secondary axis alongside the v2 line. `recomputeAndCascade()` canonical helper bundles the full scoring pipeline for API routes. Phase A (engine) shipped in PR #453; v1 snapshot rendering added in PR #504.

### Evidence Bridge (Complete)

Criteria → request item evidence propagation. When evidence is linked to a scoring criterion (manual, accept suggestion, or connector auto-link), matching request items automatically receive the evidence as a suggestion. Deterministic matching via `definition_key` = `scoring_criterion_key`. Async via the existing `propagate-evidence` SQS queue with discriminated union message routing. `EvidenceLinkBanner` conditionally renders criterion-key links ("From: [Program] · [Domain] · [key]") alongside existing semantic links ("From: [Obligation] · RI-XXX").

### Google Drive Connector (Complete)

External integration for automated evidence discovery and classification. Feature-flagged behind `google_drive` in organization metadata. OAuth-connected Google Drive sync, metadata-only file classification via LLM (Claude Haiku), auto-links evidence to scoring criteria. Watched folders, sync progress, stale file indicators, disconnect cleanup.

### Agentic File Classification (Complete)

Upgraded connector classification from single-shot to agentic Haiku with tools — folder exploration, file content preview (transient, never stored), on-demand criteria lookup. Structured triage with a 10-peek budget and fallback to single-shot on tool exhaustion. Feature-flagged per org (`agentic_classification`). OAuth scope upgrades to `drive.readonly` when enabled. Golden set expanded to 41 test cases.

### AWS Provider Swap + Cutover (Complete)

AWS migration Phase 5 — SES email (replacing Resend), Supabase storage provider removed, rate-limit sleeps removed, E2E on staging, SNS alarm notifications, DNS cutover. Inngest/Anthropic/Supabase/Resend removed, Vercel decommissioned. Cutover live 2026-03-17.

### Event-Based Score Recomputation (Complete)

Delta-based scoring pipeline replacing scattered `recomputeDomainScore()` calls. `applyScoreDelta()` as single entry point, `score_change_events` audit table, program score caching, auto-snapshots, nightly full recompute via EventBridge Lambda. All migrations applied staging and production.

---

## Planned — post-partnership

### Notifications

- Notification preferences UI — settings page to enable/disable notification types per user (P2)
- Email digest notifications — daily/weekly summary of score changes delivered via SES (P3)
- ~~`applyScoreDelta` return eventId~~ — Done in PR #360. `applyScoreDelta()` now returns `eventId` for notification→score event deep-linking (#292)

### Consent order lifecycle

- Container-level lifecycle: `issued` → `remediation_in_progress` → node-level closure → `order_lifted`
- Different from obligation lifecycle — container never fully archives
- Cross-department assignment for findings spanning multiple business lines
- Recurring deadline support via scheduled background jobs (call reports quarterly, attestations monthly)

### Bulk assignment

Select multiple request items, assign to one person. API (`PATCH /items/bulk`) + floating action bar with checkbox selection.

### ~~Member permission boundary~~ — Complete

`requireWriteAccess()` API helper enforces that members can only PATCH request items assigned to them. Admins/owners bypass. UI auto-computes read-only state with lock banner and disabled controls. Done in PR #317.

### Progress report generation

Structured quarterly progress reports for regulators. Planned for consent order support.

---

## Library evidence classification — deferred

- Auto-refresh criteria embeddings when `SCORING_CRITERIA_DEFINITIONS` change (re-run backfill or incremental update)
- Retroactive classification of existing library evidence uploaded before this feature
- Manual "re-classify" button in evidence library UI
- Reclassification when criteria definitions are updated (shared concern with connector reclassification below)

---

## Google Drive connector — V2 deferred

- Automatic polling — EventBridge scheduled sync (hourly/daily per connector)
- ~~Content-aware classification~~ — replaced by agentic classification (spec #33, `agentic_classification` feature flag)
- SharePoint / OneDrive / Box connectors — extend ConnectorProvider interface
- Folder-to-domain manual mapping override — bypass LLM classification for known folder structures
- Multi-account per org — allow multiple Google accounts connected to one organization
- Reclassification on criteria change — re-run classification when scoring criteria definitions are updated
- Confirm endpoint: refetch from server after confirm
- Coverage grid: server-side aggregated counts

---

## Infrastructure

### Performance

- ~~Database indexes on `session(user_id)`, `invitation(organization_id, email)`, `audit_log(org_id, created_at)`~~ — Done in PR #267
- ~~Partial unique indexes to prevent duplicate linked accounts and pending invites~~ — Done
- ~~GIN index on `scoring_criteria.linked_evidence` for evidence library queries~~ — Done in PR #270
- ~~Index on `evidence(org_id)` for evidence library filtering~~ — Done
- Remove Voyage AI rate-limit sleep after upgrading from free tier (parsing drops from ~10 min to ~2 min)
- Evidence library: `linkedCriteriaCount` correlated subquery scans all criteria JSONB per evidence row — add GIN index on `scoring_criteria.linked_evidence` or materialize the count when scale warrants
- Evidence library: DELETE loads all org criteria for JSONB cleanup — use `linked_evidence @>` containment query with GIN index at scale
- Add index on `evidence(org_id)` — every evidence library query filters on it

### ~~Auth and tenancy~~ — Mostly Complete

- ~~Optional org at signup~~ — Done in PR #317 (invite-only users can sign up without creating an org)
- ~~Org slug collision handling~~ — Done in PR #317 (auto-numeric suffix retry via server-side endpoint)
- ~~Cross-tenant isolation tests~~ — Done in PR #317 (~15 E2E test cases covering obligations, programs, domains, evidence, remediation)
- Atomic signup (single server-side endpoint that rolls back on failure)

### Accessibility

- `Input` component: `React.forwardRef` for form library compatibility
- `ProgressBar`: optional `ariaLabel` / `ariaValueText` props
