---
title: Build tracker
description: Active build items, planned features, and infrastructure improvements
---

Track what's being built, what's next, and what's deferred. This page mirrors the main repository's `TODO.md` and is updated via the `/sync-from-app` skill.

---

## Active — Runway Partnership

Partnership start: March 3, 2026. 60-day review: ~May 2. End: ~June 1.

### Domain-level evidence upload — Complete

Multi-file upload directly on domain detail pages. Supports drag-and-drop and maps files to the parent domain's scoring criteria.

### Score history snapshots — Complete

Point-in-time score capture with comparison mode. View how readiness scores change over time across domains.

### Evidence library — Complete

Centralised org-wide evidence view with context-free upload, filters, download, and delete.

### Readiness report export — Complete

Export compliance readiness reports as PDF binders or pillar-organised ZIP archives from the dashboard.

### Create program wizard + editing — Complete

Self-service program creation for any regulation type in `REGULATION_CONFIG`. Includes a 4-step wizard (regulation type selection, domain configuration, criteria preview with template population, baseline assessment), programs management page, and program settings page with rename, domain toggle, and delete.

---

## Planned and deferred

### Notifications

- Notification preferences UI — settings page to enable/disable notification types per user (P2)
- Email digest notifications — daily/weekly summary of score changes delivered via SES (P3)
- ~~`applyScoreDelta` return eventId~~ — Done in PR #360 (#292)

### Evidence library test coverage

- Unit tests for API routes (GET list, POST upload, DELETE, GET download)
- E2E tests (upload, filter, delete, download)
- ~~Extract shared upload component from `EvidenceUploadSection` and `DomainUploadZone`~~ — Done in PR #271

### ~~Evidence table rename~~ — Complete

Renamed `evidence_attachments` to `evidence` in PR #214.

### ~~Schema rename: examinations to obligations~~ — Complete

Renamed `examinations` table to `obligations` in PR #304. Cascaded to uploads, correspondence, FK columns, and storage paths.

### ~~Member permission boundary~~ — Complete

`requireWriteAccess()` API helper enforces that members can only PATCH items assigned to them. UI read-only mode with lock banner for non-assigned members. Done in PR #317.

### ~~Auth tenancy hardening~~ — Complete

Org slug collision handling with auto-numeric suffix retry. Optional org at signup for invite-only users. Cross-tenant isolation E2E tests (~15 test cases). Done in PR #317.

See the [Roadmap](/developers/roadmap) for planned post-partnership features (consent order lifecycle, bulk assignment), infrastructure improvements, and new platform work (desktop app, AWS migration).

---

## Scoring refactor — deferred

- Variable per-criterion weights — multiply novelty contribution by weight factor. Currently all criteria are equally weighted. P2.
- Evidence quality indicators in UI — show "low novelty" hints when evidence is highly similar to existing. Help users upload diverse evidence. P3.
- Migrate connector MD5 checksums (`sync-connector.ts` `createHash("md5")`) to SHA-256 for consistency with evidence `content_hash`. Internal cleanup.

---

## Funnel eval calibration — deferred

- **Staging `seed-eval-org --check` smoke job in `deploy.yml`.** The `--check` CLI is built and unit-tested but not yet wired into CI. Wiring requires either a thin `docker/check-eval-org.mjs` that ships in the app image and runs via `aws ecs run-task` (because GHA runners can't reach private RDS), or a Lambda. Deferred until drift becomes a recurring issue — the deploy-time backfill (which does run via `run-task`) provides the actual healing, so the smoke job is verification only.

---

## Library evidence classification — deferred

- Auto-refresh criteria embeddings when `SCORING_CRITERIA_DEFINITIONS` change (re-run backfill or incremental update)
- Retroactive classification of existing library evidence uploaded before this feature
- Manual "re-classify" button in evidence library UI
- Reclassification when criteria definitions are updated (shared concern with connector reclassification below)

---

## Google Drive connector — V2 deferred

- Automatic polling — EventBridge scheduled sync (hourly/daily per connector)
- ~~Content-aware classification~~ — replaced by agentic classification (spec #33, `agentic_classification` feature flag)
- SharePoint / OneDrive / Box connectors — extend ConnectorProvider interface
- Folder-to-domain manual mapping override — bypass LLM classification for known folder structures
- Multi-account per org — allow multiple Google accounts connected to one organization
- Reclassification on criteria change — re-run classification when scoring criteria definitions are updated
- Confirm endpoint: refetch from server after confirm
- Coverage grid: server-side aggregated counts
- Validate metadata-only classification accuracy (0.70 threshold) with real production data at 2-week post-launch mark — synthetic golden set may not represent real 403 fallback quality

---

## Desktop app — remaining phases

Phases 1-7, 10-11 complete. Remaining work blocked on Apple Developer enrollment ($99/year).

### Phase 0: Apple Developer enrollment

- Enroll in Apple Developer Program (individual, $99/year)
- Begin D-U-N-S registration for organization enrollment (parallel track)
- Generate Developer ID Application certificate and signing keys

### Phase 8: auto-updates (blocked on Phase 0)

- Configure Tauri updater endpoint and public key
- Host update manifest on S3 or static file
- Check for updates on launch + "Update now" button with progress

### Phase 9: CI/CD pipeline (blocked on Phase 0)

- GitHub Actions workflow triggered on `desktop/**` changes and `desktop-v*` tags
- Build on `macos-latest`, code sign, notarize, produce universal .dmg
- Upload to GitHub Releases, generate update manifest with signature

### Phase 10: manual testing (blocked on signed build)

- First launch, browser auth, subsequent launch with auto-refresh
- Sign out clears keychain, shows onboarding
- Offline mode with cached dashboard and amber banner
- Clean install from .dmg on fresh Mac, Gatekeeper approval
- Bundle size under 20MB

### Desktop — low priority

- Web UI "Sign Out" button in desktop mode should navigate to onboarding instead of login page
- BetterAuth 401 console errors in desktop mode (cosmetic — no BetterAuth session exists)
